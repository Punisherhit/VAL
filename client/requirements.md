## Packages
framer-motion | Essential for page transitions and floating hearts animations
lucide-react | For iconography (hearts, music notes, navigation)
wouter | Lightweight routing (already in stack but good to confirm)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  handwriting: ["var(--font-handwriting)"],
}
